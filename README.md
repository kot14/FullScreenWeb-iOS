# FullScreenWeb-iOS
FullScreenWeb is an iOS app that opens web pages in fullscreen on an external monitor or TV. You pick sites from shortcuts on your iPhone, control browsing with a captured mouse pointer, and tweak scale/offset so the page fits the display (including overscan). Optional address bar and basic ad blocking are included.
